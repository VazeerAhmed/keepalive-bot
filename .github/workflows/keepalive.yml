name: Keep Render Apps Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping YouTube Music app
        run: curl -s https://youtubemusic-uk7w.onrender.com/
      - name: Ping Portfolio site
        run: curl -s https://vazeer-portfolio.onrender.com/
      - name: Ping TELUGUFLIX site
        run: https://teluguflix.onrender.com

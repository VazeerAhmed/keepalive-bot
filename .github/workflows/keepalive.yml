name: Keep Render App Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep it awake ☕
        run: curl -s https://youtubemusic-uk7w.onrender.com/
        run: curl -s https://vazeer-portfolio.onrender.com/
